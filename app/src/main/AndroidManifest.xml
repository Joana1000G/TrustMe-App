<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trustme.trustme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrustMe">
        <activity android:name=".InstitutionInformationActivity"></activity>
        <activity android:name=".WriteHistoryActivity" />
        <activity android:name=".DonationActivity" />
        <activity android:name=".OneHistoryActivity" />
        <activity android:name=".InstitutionsActivity" />
        <activity android:name=".FeedActivity" />
        <activity android:name=".PeopleActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>